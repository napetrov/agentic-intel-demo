# Legacy Note

The repo still contains historical materials for a raw control-plane / session-pod-template path.

These files are not the canonical way to manage OpenClaw instances anymore.

Use `openclaw-operator` + `OpenClawInstance` as the only supported instance lifecycle path.

Legacy materials remain only as implementation history or supporting reference while the repo is being fully cleaned up.
