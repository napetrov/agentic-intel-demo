# Legacy Implementation Material

This tree contains deprecated implementation material from the earlier direct-managed control-plane/session-pod path.

Keep it only for reference while transitioning the repository to a cleaner operator-first demo shape.
